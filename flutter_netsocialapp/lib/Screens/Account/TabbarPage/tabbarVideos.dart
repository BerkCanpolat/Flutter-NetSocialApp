import 'package:flutter/material.dart';

class TabbarVideosScreen extends StatelessWidget {
  const TabbarVideosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}