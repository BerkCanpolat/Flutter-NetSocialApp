import 'package:flutter/material.dart';

class TabbarTaggedScreen extends StatelessWidget {
  const TabbarTaggedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}